<section style="margin-bottom: 60px; overflow: hidden; padding: 10px 0;">
    <h2 class="section-title" style="font-size: clamp(1.8rem, 5vw, 2.5rem); margin-bottom: 30px; display: flex; align-items: center; gap: 15px; color: var(--white);">
        <span style="color: var(--flutter-cyan);">03.</span> Experience 
    </h2>

    <div class="experience-list" style="box-sizing: border-box; width: 100%;">
        <% data.experience.forEach(function(exp, index) { %>
            <div class="project-card reveal" style="box-sizing: border-box; width: 100%;">
                
                <div class="exp-header">
                    <div class="exp-title-box">
                        <a href="<%= exp.website %>" target="_blank" class="company-link">
                            <%= exp.company %> <i class="fas fa-external-link-alt"></i>
                        </a>
                        <p class="exp-role">
                            <%= exp.role %>
                        </p>
                    </div>

                    <div class="exp-duration">
                        <span><%= exp.duration %></span>
                    </div>
                </div>

                <div class="exp-details-box">
                    <p class="exp-text">
                        <%= exp.details %>
                    </p>
                </div>

                <div class="card-glow"></div>
            </div>
        <% }); %>
    </div>
</section>

<style>
    .experience-list {
        display: flex; 
        flex-direction: column; 
        gap: 20px;
        width: 100%;
    }

    /* Base Card Style */
    .project-card {
        padding: 30px; 
        display: block; 
        position: relative;
        background: rgba(255, 255, 255, 0.02);
        border: 1px solid rgba(255, 255, 255, 0.05);
        border-left: 4px solid rgba(255, 255, 255, 0.1);
        border-radius: 16px;
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        overflow: hidden;
        min-width: 0; /* Ensures content wrapping inside dynamic flexboxes */
    }

    .exp-header {
        display: flex; 
        justify-content: space-between; 
        align-items: flex-start; 
        margin-bottom: 12px; 
        flex-wrap: wrap; 
        gap: 15px;
    }

    .exp-title-box {
        min-width: 0; /* Prevents long titles from blowing out parent container sizes */
        flex: 1 1 250px;
    }

    .company-link {
        color: var(--flutter-cyan); 
        font-weight: 800; 
        text-decoration: none; 
        font-size: clamp(1.3rem, 4vw, 1.8rem); 
        display: inline-flex; /* Use inline-flex for proper inline icon vertical sorting */
        align-items: center; 
        gap: 10px;
        transition: color 0.3s ease;
        word-break: break-word; /* Safe tracking against container overflowing boundaries */
    }

    .company-link i {
        font-size: 0.8rem; 
        opacity: 0.5;
        flex-shrink: 0;
    }

    .company-link:hover {
        color: #fff !important;
    }

    .exp-role {
        font-weight: 700; 
        font-size: 1.05rem; 
        color: #fff; 
        margin-top: 8px; 
        margin-bottom: 0;
        letter-spacing: 0.5px;
        word-break: break-word;
    }

    .exp-duration {
        background: rgba(255, 255, 255, 0.05); 
        padding: 8px 16px; 
        border-radius: 10px; 
        border: 1px solid rgba(255, 255, 255, 0.1);
        flex-shrink: 0;
        max-width: 100%;
        box-sizing: border-box;
    }

    .exp-duration span {
        font-family: 'Space Mono', monospace; 
        font-size: 0.85rem; 
        color: var(--flutter-cyan); 
        font-weight: bold;
        white-space: nowrap;
    }

    .exp-details-box {
        border-left: 3px solid rgba(2, 123, 227, 0.3); 
        padding-left: 20px; 
        margin-top: 25px;
        box-sizing: border-box;
    }

    .exp-text {
        color: #aaa; 
        font-size: 0.95rem; 
        line-height: 1.8; 
        white-space: pre-line;
        margin: 0;
        word-break: break-word;
    }

    /* Hover Interaction */
    @media (min-width: 992px) {
        .project-card:hover {
            border-left: 8px solid var(--flutter-cyan);
            background: rgba(2, 123, 227, 0.04);
            padding-left: 45px;
            transform: translateX(10px);
        }
    }

    /* Mobile Responsive Adjustments */
    @media (max-width: 768px) {
        .project-card {
            padding: 20px;
            border-left-width: 4px;
        }
        
        .exp-header {
            flex-direction: column;
            gap: 12px;
        }

        .exp-duration {
            align-self: flex-start;
            padding: 4px 12px;
        }

        .exp-details-box {
            padding-left: 15px;
            margin-top: 15px;
        }

        .exp-text {
            font-size: 0.9rem;
        }
    }

    @media (max-width: 400px) {
        .project-card {
            padding: 16px 12px; /* Smooth inner spacing fallback for small displays */
        }
        .exp-details-box {
            padding-left: 10px;
        }
        .exp-duration span {
            font-size: 0.75rem; /* Scales text metrics slightly down to prevent forced breakings */
        }
    }

    /* Print Logic */
    @media print {
        .project-card {
            background: #000 !important;
            border: 1px solid #333 !important;
            page-break-inside: avoid;
        }
    }
</style>